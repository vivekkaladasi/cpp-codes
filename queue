
#include <iostream>
#include<stack>
#include<queue>
using namespace std;


int main() {
   queue<string>q;
   q.push("first");
   q.push("second");
   q.push("third");
   cout<<endl;
    cout<<"size before pop"<<q.size()<<endl;
   cout<<"first element in this stack is -> "<<q.front();
    q.pop();
    cout<<endl;
    cout<<"first element in this stack after pop is -> "<<q.front()<<endl;
    cout<<"size after pop"<<q.size();
   return 0;
}
