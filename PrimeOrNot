#include<iostream>
using namespace std;
//0->is not prime
// 1 -> notPrime
bool isPrime(int n){
    for(int i=2;i<n;i++){
        if(n%i==0){
            return 0;
        }
        
    }
    return 1;
}
int main(){
    int n;
    cin>>n;
    if(isPrime(n)){
        cout<<"it is notPrime";
    }
    else{
        cout<<"it is prime";
    }
}
